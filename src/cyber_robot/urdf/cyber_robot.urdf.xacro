<?xml version="1.0"?>
<robot xmlns:xacro="http://ros.org/wiki/xacro" name="cyber_robot">
  <!-- Arguments -->
  <xacro:arg name="use_ignition" default="true"/>
  <!-- Include robot structure -->
  <xacro:include filename="$(find cyber_robot)/urdf/cyber_robot_base.urdf.xacro"/>
  <!-- Include plugin sets -->


 

  <!-- Conditionally load plugins-->
  <xacro:if value="$(arg use_ignition)">
    <xacro:include filename="$(find cyber_robot)/urdf/ignition_plugins.xacro"/>
  </xacro:if>
  <xacro:unless value="$(arg use_ignition)">
    <xacro:include filename="$(find cyber_robot)/urdf/classic_plugins.xacro"/>
  </xacro:unless>

</robot>